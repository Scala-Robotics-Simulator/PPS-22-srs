syntax = "proto3";

package io.github.srs.protos;

message PingPong { string from = 1; }
message PingPongAck {
  string from = 1;
  string to = 2;
}

service Ponger {
  rpc Ping(stream PingPong) returns (stream PingPongAck) {}
}
